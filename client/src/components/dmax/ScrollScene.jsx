/**
 * Deprecated overlay. The per-section character overlay was replaced
 * by scroll-linked motion inside each section component. Kept as a
 * no-op so existing imports don't break.
 */
export function ScrollScene() {
  return null;
}
